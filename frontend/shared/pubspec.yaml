name: shared
description: EaseInn shared package - models, API, theme, and providers
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  dio: ^5.7.0
  flutter_secure_storage: ^10.3.1
  intl: ^0.20.2
