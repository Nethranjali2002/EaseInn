name: easeinn_web
description: "EaseInn Admin Portal - Web management panel for admin and manager roles"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  shared:
    path: ../shared
  cupertino_icons: ^1.0.8
  go_router: ^17.2.3
  fl_chart: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
